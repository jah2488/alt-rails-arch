default: &default
  adapter: sqlite3
  pool: 5
  timeout: 5000

development:
  <<: *default
  database: db/development.sqlite3

test:
  <<: *default
  database: ":memory:"
  verbosity: quiet

production:
  <<: *default
  database: db/production.sqlite3
